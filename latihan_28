#include <iostream>
using namespace std;

int main () {
    bool isMale = false;
    bool isTall = true;

    if(isMale && isTall){
        cout << "You are a tall male";
    } else if (isMale && !isTall ) {
        //and is not tall

        //negasi  = !, ketika menaruh ! sebelum suatu kondisi, akan menegasikan seluruh kondisi
        cout << "You are a short male";
    } else if (!isMale && isTall){
        cout << "You are a tall but not male";
    } else {
        cout << "You are not male and not tall";
    }
    return 0;
}
*/

//bisa memeberi tahu sesuatu tergantung value dari kondisi
//perbandingan, comparison akan jadi kondisi
/*
